package com.example.filmpass.domain.screen.enums;

public enum ScreenType {
    STANDARD,   // 기본
    THREE_D,    // 3D 추가 (← 이게 없어서 에러 발생)
    FOUR_DX,    // 4DX
    IMAX,       // IMAX
    RECLINER    // 리클라이너 (눕는 좌석)
}
